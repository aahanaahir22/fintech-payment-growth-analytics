# Synthetic dataset

Run `python analysis/generate_dataset.py` to generate `synthetic_payment_transactions.csv` deterministically with seed `42`.

The generated file contains 4,000 synthetic payment attempts and is intentionally not based on proprietary company or customer data.
